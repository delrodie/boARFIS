{% extends 'backoffice.html.twig' %}

{% block body %}
    <h1>Presentations list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>Description</th>
                <th>Statut</th>
                <th>Slug</th>
                <th>Publiepar</th>
                <th>Modifiepar</th>
                <th>Publiele</th>
                <th>Modifiele</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for presentation in presentations %}
            <tr>
                <td><a href="{{ path('backend_presentation_show', { 'id': presentation.id }) }}">{{ presentation.id }}</a></td>
                <td>{{ presentation.titre }}</td>
                <td>{{ presentation.description }}</td>
                <td>{% if presentation.statut %}Yes{% else %}No{% endif %}</td>
                <td>{{ presentation.slug }}</td>
                <td>{{ presentation.publiePar }}</td>
                <td>{{ presentation.modifiePar }}</td>
                <td>{{ presentation.publieLe }}</td>
                <td>{{ presentation.modifieLe }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('backend_presentation_show', { 'id': presentation.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('backend_presentation_edit', { 'id': presentation.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('backend_presentation_new') }}">Create a new presentation</a>
        </li>
    </ul>
{% endblock %}
